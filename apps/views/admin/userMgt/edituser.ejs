<!DOCTYPE html>
<html lang="vi">

<head>
    <%- include ../layout/head %>
    <link rel="stylesheet" type="text/css" href="/static/css/signup.css">
    <title>Sửa quyền</title>
</head>

<body>
    <%- include ../layout/nav %>

    <div class="container">
        <h3>Xin chào Admin!!!</h3>

        <div id="edituserbox" style="margin-top:50px"
            class="mainbox col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
            <div class="panel panel-info">
                <div class="panel-heading">
                    <div class="panel-title">Thay đổi quyền người dùng</div>
                    <div class="panel-title">
                        <input id="btn-cancel" type="button" class="btn btn-info" value="X" />
                    </div>
                </div>

                <div class="panel-body">
                    <form id="edituser-form" class="form-horizontal" role="form" method=""
                        action="">
                        <input class="userID" type="hidden" name="userID" value="<% if(data && data.user) {%> <%= data.user.userID %> <%}%>">

                        <% if (data && data.error){%>
                        <div id="edituser-alert" class="alert alert-danger col-sm-12">
                            <%= data.error %>
                        </div>
                        <%}%>

                        <div class="form-group">
                            <label for="userID" class="col-md-3 control-label">UserID</label>
                            <div class="col-md-9">
                                <input type="number" class="form-control userID" name="userID" placeholder="<% if(data && data.user) {%> <%= data.user.userID %> <%}%>" disabled>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="username" class="col-md-3 control-label">Tên đăng nhập</label>
                            <div class="col-md-9">
                                <input type="text" class="form-control username" name="username" placeholder="<% if(data && data.user) {%> <%= data.user.username %> <%}%>" disabled>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="email" class="col-md-3 control-label">Email</label>
                            <div class="col-md-9">
                                <input type="text" class="form-control email" name="email" placeholder="<% if(data && data.user) {%> <%= data.user.email %> <%}%>" disabled>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label for="phone" class="col-md-3 control-label">Số điện thoại</label>
                            <div class="col-md-9">
                                <input type="text" class="form-control phone" name="phone" placeholder="<% if(data && data.user) {%> <%= data.user.phone %> <%}%>" disabled>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label for="address" class="col-md-3 control-label">Địa chỉ</label>
                            <div class="col-md-9">
                                <input type="text" class="form-control address" name="address" placeholder="<% if(data && data.user) {%> <%= data.user.address %> <%}%>" disabled>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="createDate" class="col-md-3 control-label">Ngày tạo</label>
                            <div class="col-md-9">
                                <input type="text" class="form-control createDate" name="createDate" placeholder="<% if(data && data.user) {%> <%= data.user.createDate %> <%}%>" disabled>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label for="roleID" class="col-md-3 control-label ">Quyền</label>
                            <div class="col-md-9">
                                <!-- <input id="roleID" type="number" class="form-control roleID" name="roleID" placeholder="Phân quyền"> -->
                                <select name="roleID" id="roleID" class="form-control roleID">
                                    <option value="0">0</option>
                                    <option value="1">1</option>
                                </select>
                            </div>
                        </div>

                        <div class="form-group">
                            <div class="col-md-offset-3 col-md-9">
                                <input id="btn-edituser" type="button" class="btn btn-success edituser" value="Cập nhật quyền" />
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script type="text/javascript" src="/static/js/userMgt.js"></script>
</body>